# Dependencies
/node_modules

# Build output — built fresh by the release workflow and shipped as a release asset
dist/

# Lockfiles for other package managers
bun.lock
yarn.lock

.DS_Store

GEMINI.md
start-test-env.sh

# Home Assistant local test configuration
/test/ha-config

# Playwright
test-results/
playwright-report/

# Local machine compose override (corporate CA, local paths)
docker-compose.override.yml
